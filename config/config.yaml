server:
  port: 8080
  mode: debug  # debug/release

database:
  driver: mysql
  host: localhost
  port: 3306
  username: root
  password: root
  database: bico_admin
  charset: utf8mb4
  max_idle_conns: 10
  max_open_conns: 100

log:
  level: info
  format: json
  output: stdout
