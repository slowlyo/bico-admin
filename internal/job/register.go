package job

// RegisterJobs 注册所有定时任务
func RegisterJobs() {
	// TODO: 使用调度器注册任务
	// 示例：
	// scheduler.Register("0 0 * * *", &task.CleanTask{})
	// scheduler.Register("*/5 * * * *", &task.SyncTask{})
}
